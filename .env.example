# Required in every environment. Generate a long random value and keep it secret.
JWT_SECRET=replace-with-a-long-random-secret

# Optional. Comma-separated browser origins allowed to call the API cross-origin.
# The deployed UI uses the same origin by default, so this can remain empty.
CORS_ORIGINS=https://your-app.up.railway.app
